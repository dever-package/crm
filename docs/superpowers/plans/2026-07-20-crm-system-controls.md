# CRM 系统控件收敛实施计划

> 财务/统计部分已由 `2026-07-20-task-capability-simplification.md` 取代。任务财务/统计关系仅保留历史审计，当前运行时读取 `DataField` 字段能力。

> 本计划在当前会话内直接执行；项目明确禁止 build/test，因此只做格式、JSON、SQL 和差异静态检查。

1. 增加任务财务类型关系、客户跟进开关和财务流水来源键模型。
2. 在任务高级配置增加财务类型复选项和客户跟进开关。
3. 生成财务金额及客户跟进固定字段，并在任务最终提交事务内直接同步业务表。
4. 将日历客户跟进操作从数据模板绑定中解耦。
5. 移除系统用途页面、模型、Provider、展示/统计运行时和前端死类型。
6. 增加幂等迁移：复制有效配置、停用被替代字段、回填新索引、保留历史业务数据。
7. 执行 `gofmt`、JSON 解析、SQL 语法/幂等核对、`rg` 残留检查和 `git diff --check`，不执行构建或测试。
