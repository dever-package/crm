# 任务会议自动字段实施计划

1. 在任务模型和迁移中增加 `meeting_enabled`，将完整旧映射转换为启用状态。
2. 简化任务编辑页：删除三级字段映射和表单构建 Provider，只保留一个会议开关。
3. 在任务详情装配阶段注入固定的会议开始时间、时长和公共资源字段。
4. 将会议同步入口改为读取系统保留字段，继续复用日程、参与人和资源预约服务。
5. 清理不再使用的字段映射 Provider，并更新本任务设计说明。
6. 只执行格式、JSON、差异和 Dever 规范静态检查；按项目要求不运行 build 或 test。

